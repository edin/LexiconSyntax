#!/usr/bin/env php
<?php

declare(strict_types=1);

use LexiconSyntax\Console\ConsoleApplicationFactory;

$autoload = $GLOBALS['_composer_autoload_path'] ?? dirname(__DIR__) . '/vendor/autoload.php';
require $autoload;

exit(ConsoleApplicationFactory::create()->run($argv));
